<%= form_with(model: workout_plan, local: true) do |form| %>
  <% if workout_plan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(workout_plan.errors.count, "error") %> prohibited this workout_plan from being saved:</h2>

      <ul>
      <% workout_plan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :level %>
    <%= form.text_field :level %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
